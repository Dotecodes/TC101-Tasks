#include <iostream>
#include <math.h>
using namespace std;

long superpower (long x, long y){
return pow(x,y);
}
int main (){
long x, y;
cout << " Introduce a number \n " ;
cin >> x;
cout << " Introduce a new number \n " ;
cin >> y;
cout << " Your result = " << superpower (x, y) << endl;
return 0;
}
